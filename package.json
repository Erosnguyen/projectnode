{
  "name": "project2",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "cleancode": "prettier --single-quote --trailing-comma all --write \"src/**/*.{js,json,scss}\"",
    "watch": "sass --watch src/scss/style.scss:src/public/style.css",
    "start": "node src/index.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "eros",
  "license": "ISC",
  "dependencies": {
    "express": "^4.17.1",
    "mongoose": "^6.0.7",
    "morgan": "^1.10.0",
    "prettier": "^2.4.1",
    "sass": "^1.42.1"
  },
  "devDependencies": {
    "express-handlebars": "^5.3.4"
  }
}
